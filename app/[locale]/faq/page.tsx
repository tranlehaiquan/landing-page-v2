import { CTA, FAQ, SocialProof } from '@/components';

export default function Page() {
    return (
        <>
            <SocialProof></SocialProof>
            <FAQ />
            <CTA />
        </>
    );
}
